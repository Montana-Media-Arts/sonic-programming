
~myTask = (
	Task({
		inf.do({
			Synth(\white_bursts);
			1.0.wait;
		});
	}).play;
)



// symbol

// always prepended with a backslash
// then followed with a single word (i.e. no spaces)
\iAmA_Symbol